# User Flow

## Typical User Journeys

- A user signs up → sets up profile → creates a training program → adds sessions and selects advanced techniques → logs workout after each session → views real-time progress on dashboard.

## Flow Diagrams
*(You may add diagrams showing program setup → logging → dashboard view later.)*

## Data Flow
- User input (log, edit, or view data) → API (process & validate) → Database (store or retrieve) → Response to frontend/dashboard.

